import { getGoogleAccessToken } from "./googleCredentials";
import type { MailProvider, ProviderListOptions, ProviderMessage, ProviderMutation, ProviderSendInput, ProviderSendResult } from "./types";
const API="https://gmail.googleapis.com/gmail/v1/users/me",MAX_PARTS=100,MAX_DEPTH=20,MAX_ATTACHMENT=4_000_000;type Header={name?:string;value?:string};type Part={partId?:string;mimeType?:string;filename?:string;headers?:Header[];body?:{data?:string;attachmentId?:string;size?:number};parts?:Part[]};type GmailMessage={id?:string;threadId?:string;labelIds?:string[];snippet?:string;internalDate?:string;payload?:Part};const enc=new TextEncoder();
const bytesToBase64=(bytes:Uint8Array)=>{let out="";for(let i=0;i<bytes.length;i+=0x8000)out+=String.fromCharCode(...bytes.subarray(i,i+0x8000));return btoa(out)};const b64url=(bytes:Uint8Array)=>bytesToBase64(bytes).replace(/\+/g,"-").replace(/\//g,"_").replace(/=+$/g,"");const decodeBytes=(value:string)=>{const s=value.replace(/-/g,"+").replace(/_/g,"/");const raw=atob(s+"=".repeat((4-s.length%4)%4)),bytes=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)bytes[i]=raw.charCodeAt(i);return bytes};const decode=(v:string)=>new TextDecoder().decode(decodeBytes(v));const header=(m:GmailMessage,n:string)=>m.payload?.headers?.find(h=>h.name?.toLowerCase()===n.toLowerCase())?.value??"";const addr=(v:string)=>{const m=v.match(/^(.*?)\s*<([^>]+)>$/);return m?{name:m[1].replace(/^"|"$/g,"").trim()||null,email:m[2].trim()}:{name:null,email:v.trim()}};const addresses=(v:string)=>v.split(",").map(x=>addr(x).email).filter(Boolean);
function parts(part:Part|undefined,out:Part[]=[],depth=0){if(!part||depth>MAX_DEPTH||out.length>=MAX_PARTS)return out;if(part.mimeType?.startsWith("text/")||part.filename||part.body?.attachmentId)out.push(part);for(const p of part.parts??[])parts(p,out,depth+1);return out}function text(m:GmailMessage){const all=parts(m.payload),plain=all.find(p=>p.mimeType==="text/plain"&&p.body?.data),html=all.find(p=>p.mimeType==="text/html"&&p.body?.data),raw=plain?.body?.data?decode(plain.body.data):html?.body?.data?decode(html.body.data).replace(/<style[\s\S]*?<\/style>/gi,"").replace(/<script[\s\S]*?<\/script>/gi,"").replace(/<[^>]+>/g," ").replace(/&nbsp;/g," ").replace(/&amp;/g,"&").replace(/&lt;/g,"<").replace(/&gt;/g,">"):"";return raw.replace(/\s+\n/g,"\n").trim().slice(0,2_000_000)}function item(m:GmailMessage):ProviderMessage{const labels=new Set(m.labelIds??[]),from=addr(header(m,"From")),sent=labels.has("SENT"),date=new Date(Number(m.internalDate)||Date.now()).toISOString();return{id:m.id??"",threadId:m.threadId??m.id??"",direction:sent?"outbound":"inbound",fromAddress:from.email,fromName:from.name,toAddresses:addresses(header(m,"To")),subject:header(m,"Subject")||"(no subject)",preview:m.snippet??"",receivedAt:date,sentAt:sent?date:null,isRead:!labels.has("UNREAD"),isStarred:labels.has("STARRED"),isArchived:!labels.has("INBOX")&&!labels.has("SENT")&&!labels.has("TRASH"),isDeleted:labels.has("TRASH"),hasAttachments:parts(m.payload).some(p=>Boolean(p.filename&&p.body?.attachmentId)),deliveryStatus:null,isDraft:false}}
function clean(v:string){return v.replace(/[\r\n]+/g," ").trim()}const validHeader=(v:string)=>/^[A-Za-z0-9-]{1,64}$/.test(v);const quoted=(v:string)=>`"${clean(v).replace(/["\\]/g,"\\$&")}"`;function mime(input:ProviderSendInput,from:string){const boundary=`ltm_${crypto.randomUUID().replace(/-/g,"")}`,lines=[`From: ${clean(from)}`,`To: ${input.to.map(clean).join(", ")}`];if(input.cc.length)lines.push(`Cc: ${input.cc.map(clean).join(", ")}`);if(input.bcc.length)lines.push(`Bcc: ${input.bcc.map(clean).join(", ")}`);lines.push(`Subject: ${clean(input.subject)}`,"MIME-Version: 1.0");for(const[k,v]of Object.entries(input.headers??{}))if(validHeader(k))lines.push(`${k}: ${clean(v)}`);if(!input.attachments.length){lines.push('Content-Type: text/plain; charset="UTF-8"',"Content-Transfer-Encoding: 8bit","",input.text);return lines.join("\r\n")}lines.push(`Content-Type: multipart/mixed; boundary="${boundary}"`,"",`--${boundary}`,'Content-Type: text/plain; charset="UTF-8"',"Content-Transfer-Encoding: 8bit","",input.text);for(const a of input.attachments){const type=/^[\w.+-]+\/[\w.+-]+$/.test(a.type)?a.type:"application/octet-stream",data=bytesToBase64(new Uint8Array(a.content)).replace(/.{1,76}/g,"$&\r\n").trimEnd();lines.push(`--${boundary}`,`Content-Type: ${type}; name=${quoted(a.filename)}`,"Content-Transfer-Encoding: base64",`Content-Disposition: attachment; filename=${quoted(a.filename)}`,"",data)}lines.push(`--${boundary}--`,"");return lines.join("\r\n")}
async function call(env:Env,accountId:string,path:string,init:RequestInit={}){const token=await getGoogleAccessToken(env,accountId),headers=new Headers(init.headers);headers.set("Authorization",`Bearer ${token}`);const r=await fetch(`${API}${path}`,{...init,headers});if(!r.ok)throw new Error(r.status===401?"Gmail account requires reconnection":"Gmail request failed");return r}const q=(f:string)=>f==="inbox"?"in:inbox":f==="starred"?"is:starred":f==="sent"?"in:sent":f==="trash"?"in:trash":f==="archive"?"-in:inbox -in:sent -in:trash -in:spam -in:drafts":"";
export async function gmailFullMessage(env:Env,accountId:string,id:string){const m=await(await call(env,accountId,`/messages/${encodeURIComponent(id)}?format=full`)).json<GmailMessage>(),base=item(m),attachments=parts(m.payload).filter(p=>p.filename&&p.body?.attachmentId).map(p=>({id:p.body!.attachmentId!,filename:p.filename!,contentType:p.mimeType||"application/octet-stream",size:Math.max(0,p.body?.size??0)}));return{...base,ccAddresses:addresses(header(m,"Cc")),bccAddresses:addresses(header(m,"Bcc")),bodyText:text(m)||base.preview,bodyHtmlAvailable:false,inReplyTo:header(m,"In-Reply-To")||null,references:header(m,"References")||null,messageId:header(m,"Message-ID")||null,deliveryError:null,attachments}}
export async function gmailThread(env:Env,accountId:string,threadId:string){const t=await(await call(env,accountId,`/threads/${encodeURIComponent(threadId)}?format=full`)).json<{messages?:GmailMessage[]}>();return (t.messages??[]).slice(-100).map(m=>{const base=item(m);return{...base,ccAddresses:addresses(header(m,"Cc")),bccAddresses:addresses(header(m,"Bcc")),bodyText:text(m)||base.preview,bodyHtmlAvailable:false,inReplyTo:header(m,"In-Reply-To")||null,references:header(m,"References")||null,messageId:header(m,"Message-ID")||null,deliveryError:null,attachments:parts(m.payload).filter(p=>p.filename&&p.body?.attachmentId).map(p=>({id:p.body!.attachmentId!,filename:p.filename!,contentType:p.mimeType||"application/octet-stream",size:Math.max(0,p.body?.size??0)}))}})}
export async function gmailAttachment(env:Env,accountId:string,messageId:string,attachmentId:string){const m=await(await call(env,accountId,`/messages/${encodeURIComponent(messageId)}?format=full`)).json<GmailMessage>(),part=parts(m.payload).find(p=>p.body?.attachmentId===attachmentId&&p.filename);if(!part||!part.filename)throw new Error("Attachment not found");if((part.body?.size??0)>MAX_ATTACHMENT)throw new Error("Attachment too large");const r=await(await call(env,accountId,`/messages/${encodeURIComponent(messageId)}/attachments/${encodeURIComponent(attachmentId)}`)).json<{data?:string;size?:number}>(),data=r.data?decodeBytes(r.data):new Uint8Array();if(data.byteLength>MAX_ATTACHMENT)throw new Error("Attachment too large");return{data,filename:part.filename,contentType:part.mimeType||"application/octet-stream"}}
export function gmailProvider(env:Env,accountId:string,email:string):MailProvider{return{kind:"gmail",accountId,address:email,async listMessages(o:ProviderListOptions){const query=[q(o.folder),o.search].filter(Boolean).join(" "),l=await(await call(env,accountId,`/messages?maxResults=${Math.min(o.limit,50)}&q=${encodeURIComponent(query)}`)).json<{messages?:Array<{id:string}>}>(),out:ProviderMessage[]=[];for(const x of l.messages??[]){const m=await(await call(env,accountId,`/messages/${encodeURIComponent(x.id)}?format=metadata&metadataHeaders=From&metadataHeaders=To&metadataHeaders=Subject`)).json<GmailMessage>();out.push(item(m))}return out},async getMessage(id){try{return item(await(await call(env,accountId,`/messages/${encodeURIComponent(id)}?format=metadata&metadataHeaders=From&metadataHeaders=To&metadataHeaders=Subject`)).json<GmailMessage>())}catch{return null}},async patchMessage(id,m:ProviderMutation){if(m.isDeleted!==undefined){await call(env,accountId,`/messages/${encodeURIComponent(id)}/${m.isDeleted?"trash":"untrash"}`,{method:"POST"});return true}const add:string[]=[],remove:string[]=[];if(m.isRead!==undefined)(m.isRead?remove:add).push("UNREAD");if(m.isStarred!==undefined)(m.isStarred?add:remove).push("STARRED");if(m.isArchived!==undefined)(m.isArchived?remove:add).push("INBOX");await call(env,accountId,`/messages/${encodeURIComponent(id)}/modify`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({addLabelIds:add,removeLabelIds:remove})});return true},async sendMessage(input:ProviderSendInput):Promise<ProviderSendResult>{if(input.attachments.reduce((n,a)=>n+a.content.byteLength,0)>MAX_ATTACHMENT)throw new Error("Attachments too large");const raw=b64url(enc.encode(mime(input,email))),body:Record<string,string>={raw};if(input.threadId)body.threadId=input.threadId;const r=await(await call(env,accountId,"/messages/send",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)})).json<{id?:string}>();return{providerMessageId:r.id??null}}}}
