import { getGoogleAccessToken } from "../providers/googleCredentials";
const ALLOWED=new Set(["image/jpeg","image/png","image/webp"]),MAX=2*1024*1024;
export async function getAccountAvatar(accountId:string,env:Env):Promise<Response>{if(!accountId.startsWith("gmail:"))return new Response(null,{status:404});const row=await env.DB.prepare("SELECT status FROM connected_accounts WHERE id=?1 AND provider='gmail' LIMIT 1").bind(accountId).first<{status:string}>();if(!row||row.status!=="active")return new Response(null,{status:404});try{const token=await getGoogleAccessToken(env,accountId);const user=await fetch("https://www.googleapis.com/oauth2/v3/userinfo",{headers:{Authorization:`Bearer ${token}`}});if(!user.ok)return new Response(null,{status:404});const info=await user.json<{picture?:string}>();if(!info.picture)return new Response(null,{status:404});const picture=new URL(info.picture);if(picture.protocol!=="https:"||!(picture.hostname==="lh3.googleusercontent.com"||picture.hostname.endsWith(".googleusercontent.com")))return new Response(null,{status:404});const image=await fetch(picture.toString(),{redirect:"error"});if(!image.ok)return new Response(null,{status:404});const type=(image.headers.get("Content-Type")??"").split(";")[0].trim().toLowerCase();const declared=Number(image.headers.get("Content-Length")??0);if(!ALLOWED.has(type)||(declared&&declared>MAX))return new Response(null,{status:404});const bytes=await image.arrayBuffer();if(bytes.byteLength>MAX)return new Response(null,{status:404});return new Response(bytes,{headers:{"Content-Type":type,"Content-Length":String(bytes.byteLength),"Cache-Control":"private, max-age=3600","X-Content-Type-Options":"nosniff"}})}catch{return new Response(null,{status:404})}}
