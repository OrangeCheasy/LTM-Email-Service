import { getConnectedAccounts } from "./api/accounts";import { getAccountAvatar } from "./api/accountAvatar";import { getContactAvatar } from "./api/contactAvatar";import { previewAttachment } from "./api/attachments";import { deleteDraft,getDraft,saveDraft } from "./api/drafts";import { healthResponse } from "./api/health";import { downloadAttachment,getMessage,listMessages,patchMessage } from "./api/mail";import { deleteProfilePhoto,getProfile,getProfilePhoto,saveProfilePhoto } from "./api/profile";import { authenticationOptions,authStatus,isAuthenticated,logout,registrationOptions,resetSessions,sessionInfo,unauthorizedResponse,verifyAuthentication,verifyRegistration,verifySmsSecondFactor } from "./auth";import { APP_ORIGIN } from "./config";import { receiveEmail } from "./email/receive";import { sendEmail } from "./email/send";import { disconnectGoogle,googleOAuthCallback,startGoogleOAuth } from "./providers/google";import { pushConfig,subscribePush,unsubscribePush } from "./push";import { authRateLimitBucket,checkAuthRateLimit } from "./security";
const SAFE_METHODS=new Set(["GET","HEAD","OPTIONS"]),AUTH_POSTS=new Set(["/api/auth/register/options","/api/auth/register/verify","/api/auth/login/options","/api/auth/login/verify","/api/auth/sms/verify"]),MAX_API_BODY_BYTES=8*1024*1024;const jsonError=(message:string,status:number)=>Response.json({error:message},{status}),sameOrigin=(request:Request)=>SAFE_METHODS.has(request.method)||request.headers.get("Origin")===APP_ORIGIN;function requestTooLarge(request:Request){const raw=request.headers.get("Content-Length");if(!raw)return false;const length=Number(raw);return Number.isFinite(length)&&length>MAX_API_BODY_BYTES}function secureApiResponse(response:Response){const headers=new Headers(response.headers);headers.set("Cache-Control","private, no-store");headers.set("Content-Security-Policy","default-src 'none'; frame-ancestors 'none'; base-uri 'none'");headers.set("Referrer-Policy","no-referrer");headers.set("X-Content-Type-Options","nosniff");headers.set("X-Frame-Options","DENY");return new Response(response.body,{status:response.status,statusText:response.statusText,headers})}function decodePathSegment(value:string){try{return decodeURIComponent(value)}catch{return null}}
async function routeApi(request:Request,env:Env,url:URL):Promise<Response>{if(!sameOrigin(request))return jsonError("Cross-origin request rejected",403);if(!SAFE_METHODS.has(request.method)&&requestTooLarge(request))return jsonError("Request body too large",413);if(request.method==="POST"&&AUTH_POSTS.has(url.pathname)&&!(await checkAuthRateLimit(env,authRateLimitBucket(request,url.pathname))))return new Response(JSON.stringify({error:"Too many authentication attempts"}),{status:429,headers:{"Content-Type":"application/json","Retry-After":"60"}});if(url.pathname==="/api/auth/status"&&request.method==="GET")return authStatus(request,env);if(url.pathname==="/api/auth/register/options"&&request.method==="POST")return registrationOptions(request,env);if(url.pathname==="/api/auth/register/verify"&&request.method==="POST")return verifyRegistration(request,env);if(url.pathname==="/api/auth/login/options"&&request.method==="POST")return authenticationOptions(request,env);if(url.pathname==="/api/auth/login/verify"&&request.method==="POST")return verifyAuthentication(request,env);if(url.pathname==="/api/auth/sms/verify"&&request.method==="POST")return verifySmsSecondFactor(request,env);if(url.pathname==="/api/auth/logout"&&request.method==="POST")return logout(request,env);if(url.pathname==="/api/accounts/gmail/callback"&&request.method==="GET")return googleOAuthCallback(request,env);if(!(await isAuthenticated(request,env)))return unauthorizedResponse();if(url.pathname==="/api/auth/sessions"&&request.method==="GET")return sessionInfo(request,env);if(url.pathname==="/api/auth/sessions/reset"&&request.method==="POST")return resetSessions(request,env);if(url.pathname==="/api/accounts/gmail/connect"&&request.method==="POST")return startGoogleOAuth(env);if(url.pathname==="/api/contact-avatar"&&request.method==="GET")return getContactAvatar(request,env);const accountRoute=url.pathname.match(/^\/api\/accounts\/([^/]+)$/);if(accountRoute&&request.method==="DELETE"){const accountId=decodePathSegment(accountRoute[1]);if(accountId?.startsWith("gmail:"))return disconnectGoogle(accountId,env)}const avatarRoute=url.pathname.match(/^\/api\/accounts\/([^/]+)\/avatar$/);if(avatarRoute&&request.method==="GET"){const accountId=decodePathSegment(avatarRoute[1]);if(accountId?.startsWith("gmail:"))return getAccountAvatar(accountId,env)}if(url.pathname==="/api/health"&&request.method==="GET")return healthResponse(env);if(url.pathname==="/api/accounts"&&request.method==="GET")return getConnectedAccounts(env);if(url.pathname==="/api/messages"&&request.method==="GET")return listMessages(request,env);if(url.pathname==="/api/send"&&request.method==="POST")return sendEmail(request,env);if(url.pathname==="/api/drafts"&&request.method==="POST")return saveDraft(request,env);if(url.pathname==="/api/profile"&&request.method==="GET")return getProfile(env);if(url.pathname==="/api/profile/photo"){if(request.method==="GET")return getProfilePhoto(env);if(request.method==="PUT")return saveProfilePhoto(request,env);if(request.method==="DELETE")return deleteProfilePhoto(env);return jsonError("Method not allowed",405)}if(url.pathname==="/api/push/config"&&request.method==="GET")return pushConfig(env);if(url.pathname==="/api/push/subscribe"&&request.method==="POST")return subscribePush(request,env);if(url.pathname==="/api/push/unsubscribe"&&request.method==="POST")return unsubscribePush(request,env);const draftMatch=url.pathname.match(/^\/api\/drafts\/([^/]+)$/);if(draftMatch){const id=decodeURIComponent(draftMatch[1]);if(request.method==="GET")return getDraft(id,env);if(request.method==="DELETE")return deleteDraft(id,env);return jsonError("Method not allowed",405)}const messageMatch=url.pathname.match(/^\/api\/messages\/([^/]+)$/);if(messageMatch){const id=decodeURIComponent(messageMatch[1]);if(request.method==="GET")return getMessage(request,id,env);if(request.method==="PATCH")return patchMessage(request,id,env);return jsonError("Method not allowed",405)}const previewMatch=url.pathname.match(/^\/api\/attachment-previews\/([^/]+)$/);if(previewMatch&&request.method==="GET")return previewAttachment(request,decodeURIComponent(previewMatch[1]),env);const attachmentMatch=url.pathname.match(/^\/api\/attachments\/([^/]+)$/);if(attachmentMatch&&request.method==="GET")return downloadAttachment(request,decodeURIComponent(attachmentMatch[1]),env);return jsonError("Not found",404)}
export default{async fetch(request,env):Promise<Response>{const url=new URL(request.url);if(url.pathname.startsWith("/api/"))return secureApiResponse(await routeApi(request,env,url));return env.ASSETS.fetch(request)},async email(message,env,ctx):Promise<void>{await receiveEmail(message,env,ctx)}} satisfies ExportedHandler<Env>;
